#定义服务端口号
server:
  port: 80

spring:
  application:
    name: basic
  profiles:
    active:
  # 把static的默认路径放在resources下, 把静态文件放在static文件夹下, 这样权限控制过滤静态文件的时候直接过滤static就好了
  resources:
    static-locations: classpath:/


#数据库相关配置 后续再说  看完再返回UserController
  datasource:
    url: jdbc:mysql://47.101.53.92:3306/basic?autoReconnect=true&failOverReadOnly=false&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 19941111
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 10
      min-idle: 15
      max-active: 30
      max-wait: 60000
      time-between-eviction-runs-millisr: 28000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 'x'
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: false
      max-pool-prepared-statement-per-connection-size: 20
      filters: stat,wall,slf4j
      connection-properties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
      use-global-data-source-stat: true
      remove-abandoned: true